# my-Blog
Web课程期末项目，做了个人博客

前端使用了 Vue、JQuery框架，后端是 Apache + PHP + Mysql，数据库中仅存储了todoList：
与数据库可通过修改config.php来修改配置。（私人使用的原因，仅能本地访问）

使用步骤如下：
1.	将整个项目放到Apache支持的文件夹下
2.	在Mysql中导入上述table（init.sql）
3.	修改config.php中的数据库配置
4.	开启Apache的httpd
5.	在浏览器中输入 localhost 来进入网站
